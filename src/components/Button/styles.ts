import { styled } from '../../config';

export const TouchableOpacity = styled('TouchableOpacity', {
  width: '100%',
  margin: 5,
  padding: '$3',
  alignSelf: 'center',
  alignItems: 'center',
  justifyContent: 'center',
});
