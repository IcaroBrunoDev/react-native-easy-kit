import { styled } from '../../../stitches.config';

export const Wrapper = styled('TouchableOpacity', {
  elevation: 1,
  width: 60,
  height: 60,
  borderRadius: 30,
  position: 'absolute',
  bottom: 20,
  right: 10,
  zIndex: 1050,
  display: 'flex',
  alignItems: 'center',
  justifyContent: 'center',
});
