import Button from './components/Button';

import {
  theme,
  styled,
  useTheme,
  extendtheme,
  ThemeProvider,
} from '../base.config';

export { Button };
export { theme, styled, useTheme, extendtheme, ThemeProvider };
