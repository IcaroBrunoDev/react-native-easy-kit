import { createBaseTheme } from 'rn-ui-base-components';

export const { styled, useTheme, ThemeProvider } = createBaseTheme({
  theme: {
    colors: {
      primary: '#000',
    },
    fonts: {},
  },
});
